name: test_env
channels:
  - conda-forge
dependencies:
  - python=2.7
  - pip
  - pydap
  - xarray
  - numpy
  - scipy
  - {{ compiler('fortran') }}
  - {{ compiler('c') }}
  - future
  - attrdict
  - urllib3
  - pytest
  - codecov
  - pytest-cov
